#!/bin/bash


# Java and maven
sudo apt install -y openjdk-21-jdk 

sudo apt install -y maven

# Node js
# TODO: It's probably better to use nvm for nodejs, in order to better specify version
sudo apt install -y nodejs
